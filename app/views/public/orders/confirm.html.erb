確認画面

商品名
単価(税込)
数量
小計

<% @total=0 %>
<% @cart_items.each do |cart| %>

 <%= image_tag cart.item.get_image(50,50) %>
 <%= cart.item.item_name %>
 <%= (cart.item.tax_include_price*cart.amount).to_s(:delimited) %>円
 <% @total += cart.item.tax_include_price*cart.amount %>

<% end %>

送料
商品合計
請求合計

800円
<%= @total %>円
<%= @total + 800 %>円

支払方法
<%= @order.payment_method_i18n %>

〒<%= @order.post_code %> <%= @order.address %> <%= @order.name %>

<%= form_with model: @order, url: orders_path, lical: true do |f| %>
 <%= f.hidden_field :name %>
 <%= f.hidden_field :customer_id, value: current_customer.id %>
 <%= f.hidden_field :shipping_fee, value: 800 %>
 <%= f.hidden_field :post_code %>
 <%= f.hidden_field :address %>
 <%= f.hidden_field :total_payment, value: @total + 800 %>
  <%= f.submit "注文を確定する" %>
<% end %>