<div id="genre-table">
  <table class="table">
    <thead>
      <tr>
        <th class="text-black-50 pb-0">
          ジャンル検索
        </th>
      </tr>
    </thead>
    <tbody>
      <% genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre.genre_name, item_search_items_path(genre_id: genre.id, model: "genre") %>
        </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>