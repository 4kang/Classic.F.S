商品一覧

<% @items.each do |item| %>

商品ID<%= item.id %>
商品名<%= link_to item.item_name, admin_item_path(item) %>
税抜価格<%= item.price %>
ジャンル<%= item.genre.genre_name %>
販売ステータス



<% end %>

<%= link_to '商品登録', new_admin_item_path %>